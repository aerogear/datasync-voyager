{
  "name": "@aerogear/apollo-voyager-offline",
  "version": "1.0.0",
  "description": "A package to provide offline capabilities such as recovery from data conflicts.",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc --build tsconfig.json",
    "watch": "tsc --build tsconfig.json --watch",
    "test": "ava '*.test.ts' '**/*.test.ts'",
    "compile:clean": "tsc --build tsconfig.json --clean"
  },
  "author": "AeroGear Team<aerogear@googlegroups.com>",
  "license": "Apache-2.0",
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "devDependencies": {
    "@types/debug": "0.0.31",
    "ava": "1.0.0-rc.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "@aerogear/apollo-voyager-tools": "^1.0.0",
    "debug": "^4.1.0"
  }
}
